{
  "name": "nextnx",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "nextnx",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/nextnx"
      },
      "configurations": {
        "development": {
          "outputPath": "nextnx"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "nextnx:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "nextnx:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "nextnx:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "nextnx:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "nextnx/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    }
  },
  "tags": []
}
